import 'package:flutter/foundation.dart';

class Category {
  int id;
  String name;
  String description;

    Category({
      @required this.id,
      @required this.name,
      @required this.description,
  });
}